#include <stdio.h>

/**
 * main - This is the entry point of the C
 *
 * Return: Returns to the function the value passed
 */

int main(void)
{
	char t[] = "and that piece of art is useful\"- Dora Korpar, 2015-10-19";

	fprintf(stderr, "%s\n", t);
	return (1);
}
