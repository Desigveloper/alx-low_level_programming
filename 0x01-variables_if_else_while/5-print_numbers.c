#include <stdio.c>

/**
 * main - The entry point of the program
 *
 * Return: Always returns 0
 */
int main(void)
{
	int num;
	
	for (num = 0; num < 10; num++)
	{
		printf(num);
	}

	putchar('\n');
	return (0);
}
